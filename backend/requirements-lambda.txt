# Dependencies for the AWS Lambda deployment package only.
#
# This intentionally excludes uvicorn[standard] (and its extras: uvloop, httptools,
# websockets, watchfiles) since that's a local-dev ASGI server never imported by the
# app itself — Mangum replaces its job inside Lambda. Also excludes python-multipart,
# which isn't used anywhere in this API (no file-upload/form endpoints).
#
# Build the deployment package with:
#   pip install -r requirements-lambda.txt -t package/
#
# See LAMBDA_DEPLOYMENT.md for full packaging and deployment steps.

fastapi==0.138.2
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
email-validator==2.3.0
PyJWT==2.13.0
boto3==1.43.53
mangum==0.21.0
